{
    "HAPI": "3.1",
    "outputFormats": [
        "csv"
    ],
    "x_customRequestOptions": [
	{ "name": "baseline",
	  "description": "Changing the baseline subtraction from the default",
	  "type": "string",
	  "constraint": {
	      "enum": ["system","yearly","none"]
	  },
	  "default": "system"
	},
	{ "name": "fictitious_demo",
	  "description": "Demo spec for an unused numeric sample param",
	  "type": "number",
	  "constraint": {
	      "minimum": 1,
	      "exclusiveMaximum": 100
	  },
	  "default": "system"
	},
	{ "name": "delta",
	  "description": "Changing the delta subtraction from the default",
	  "type": "string",
	  "constraint": {
	      "enum": ["system","none","start"]
	  },
	  "default": "system"
	}
    ],
    "status": {
        "code": 1200,
        "message": "OK request successful"
    }
}
